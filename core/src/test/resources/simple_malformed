def simple {
    when (
        battery (
            plugged ac,
            level > 50%
        ),
        headset connected
    ) do {
        set sound profile to vibrate
    }
}

def simple {
    when (
        xor (
            battery (
                or (
                    plugged ac,
                    level > 50%
                )
            ),
            headset connected
        )
    ) do {
        set sound profile to vibrate
    }
}
def simple_sound_off {
    when (
        headset connected,
        wifi on
    ) do {
        set sound profile to silent
    }
}

def simple_sound_on {
    when (
        headset disconnected,
        wifi off
    ) do {
        set sound profile to normal
    }
}
